using UnityEngine;

using Arielado;
using Unity.VisualScripting;

namespace Arielado.Graphs {
    public static class GraphFactory {
        /*public static T CreateGraph<T>(string graphName) {
            switch (graphName) {
                
            }
        }*/

        /*public static T CreateFromCopy<T>(IGraph graph) where T : IGraph {
            switch (graph) {
                case MeshTriangleGraph triGraph: 
                    
                    break;
            }
        }*/
    }
}
